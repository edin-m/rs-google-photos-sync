use std::result;
use std::io;
use std::convert::From;

//pub type CustomResult<T> = result::Result<T, CustomError>;
//
//pub enum CustomError {
//    Error(String)
//}
//
//impl From<io::Error> for CustomError {
//    fn from(err: io::Error) -> CustomError { CustomError::Error(err.to_string()) }
//}
